## 🎯 

- [ ] 函数
- [ ] 字符串初始化

```
#include<stdio.h>
#include<string.h>
int main()
{
	char str2[11];
	strcpy(str2,"HelloWorld");
}
```
- [ ] 计算机体系结构
![[Pasted image 20251223234425.png]]
- [ ] 内存
- [ ] 数组与sizeof
```
#include<stdio.h>
#include<string.h>
int main()
{
	int a[] = {5,6,7};
	int b = 10;
	int len = 0;
	printf("%p\n",&b);
	printf("%p\n",&a[0]);
	len = sizeof(a)/sizeof(a[0]);/*获取数组大小*/
}
```
- [ ] 指针：==**用来存内存地址的变量**==
```
int  a = 5;  //声明一个int变量；
int* p = &a；//声明一个指向int类型的指针变量，并将a的地址赋值给p；
```
- [ ] 结构体
```
typedef struct
{
	uint8 a;
	uint8 b;
}Test_Struct_S;

Test_Struct_S Test_Struct;
```
- [ ] 动态内存分配
```
typedef struct
{
	uint8 a;
	uint8 b;
}Test_Struct_S;

int main1()
{
	int* p;
	p = (int*)malloc(sizeof(int));
	*p = 15;
	printf("%d\n",*p);
	free(p);
	return 0;
}

int main2()
{
	Test_Struct_S* p;
	p = (Test_Struct_S*)malloc(sizeof(Test_Struct_S));
	p->a = 15;
	P->b = 16;
	free(p);
	return 0;
}
```