## 🎯 链表（LINKED LIST）

```
链表节点：数据域+指针域；
N个节点通过指针域相互链接；
```
- [ ] 头节点(非必需)：一般不存数据，而是存链表的长度，如果指针域为NULL,代表链表内没有节点；
![[Pasted image 20251224005218.png]]
- [ ] 头指针永远指向链表中第一个节点的位置
![[Pasted image 20251224005542.png]]
- [ ] 链表声明
```
#include <stdio.h>
#include <string.h>

#define Link_Node_NUM_MAX 100

// 定义结构体，使用标签
typedef struct LinkNode {
    int status;
    int data[100];
    struct LinkNode* next;  // ✅ 使用结构体标签
} Link_Node_S;

// 声明链表节点数组
Link_Node_S Link_Node[Link_Node_NUM_MAX] = {0};

// 初始化链表 - 创建顺序链接
void Init_Link_List() {
    for (int i = 0; i < Link_Node_NUM_MAX - 1; i++) {
        Link_Node[i].next = &Link_Node[i + 1];
    }
    // 最后一个节点的 next 设为 NULL
    Link_Node[Link_Node_NUM_MAX - 1].next = NULL;
}
```
- [ ] 链表在DiagRouterCDD中的使用
```

```